apiVersion: v1
kind: Service
metadata:
  name: {{ include "budgetbook-chart.fullname" . }}
  labels:
    {{- include "budgetbook-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "budgetbook-chart.selectorLabels" . | nindent 4 }}
